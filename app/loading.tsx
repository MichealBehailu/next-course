import React from "react";

//this page act as a root loading to all pages or component

const loading = () => {
  return <span className="loading loading-spinner loading-md"></span>;
};

export default loading;
