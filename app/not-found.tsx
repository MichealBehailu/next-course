import React from 'react'


//this page is useful if the user tries to enter page that doesnt exist and act as a root 

const NotFoundPage = () => {
  return (
    <div>The requested page doesn&apos;t exist.</div>
  )
}

export default NotFoundPage